import AdminBro from 'admin-bro';
import { AbstractHttpAdapter } from '@nestjs/core';
import { AdminModuleOptions } from '../interfaces/admin-module-options.interface';
export declare abstract class AbstractLoader {
    abstract register(admin: AdminBro, httpAdapter: AbstractHttpAdapter, options: AdminModuleOptions): any;
}
